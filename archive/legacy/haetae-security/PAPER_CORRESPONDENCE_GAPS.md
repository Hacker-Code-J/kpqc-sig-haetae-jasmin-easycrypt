# HAETAE Paper-Correspondence Gap Table

This table separates machine-checked EasyCrypt facts from places where the
current structural model still has to be aligned with the HAETAE paper and
reference implementation before claiming a full HAETAE security proof.

| Area | Current EasyCrypt status | Paper-faithful replacement needed | Risk |
| --- | --- | --- | --- |
| Module-SIS equation | `module_sis_eval` and `module_sis_valid` use concrete matrix-vector multiplication over list polynomials, and the fork-specific target is no longer the zero vector. | Define the exact HAETAE Module-SIS instance distribution, norm bounds, and hardness-game sampler, then prove the lifted bimodal distribution is covered by that sampler. | High: current validity preservation is checked, but the distributional hardness handoff is not. |
| Public key algebra and FIPS202 | `public_key_of_secret` stores a mode-shaped public vector, the raw `A0*s1+s2` equation remains named, the reference-shaped packed-key path has checked pack/unpack roundtrips, and the FIPS202 layer has concrete Keccak-f[1600] bit/byte projection infrastructure. The empty SHAKE256 KAT now has a compiled full set of round-24 output-bit premises, all output-byte bridges `empty_round24_output_byteNN_from_expected_bits`, and a final-bit-to-KAT reducer, with `tools/gen_empty_kat_bit_lemma.py` generating the concrete one-step Keccak bit lemmas. | Prove actual reference-C/Jasmin FIPS202 memory equivalence, complete the backward round-premise discharge to the padded input for all bytes, and connect the structural mode-shaped public key to real HAETAE NTT placement, rejection-loop sampling, rounding, and verifier-matrix construction. | High: the 64-byte KAT bridge is now concrete, but C/Jasmin memory equivalence, full backward chaining to input bytes, real keygen correspondence, NTT, and rejection-loop behavior remain unproved. |
| Signing equations | `sign_internal` carries deterministic nonzero response, auxiliary-response, hint, commitment, and challenge fields with checked well-formedness and norm bounds. | Model the real HAETAE signing computation: hyperball sample, commitment, challenge, response, rejection checks, hints, and accepted output distribution. | High: simulated-signing and rejection-sampling theorems cannot be paper-faithful until this is concrete. |
| Challenge distribution | `challenge_hash` produces a checked prefix-sparse structural challenge and the ROM programming bound uses a concrete 58-bit entropy token in the programmed site input. The current structural `challenge_hash` keeps the highbits argument in the interface but derives the challenge from lowbits and the message hash. | Replace this with the HAETAE challenge sampler over the full encoded transcript, then prove exact support/cardinality, point-probability, and rejection/min-entropy facts used by the paper and implementation. | High: current ROM bounds are machine-checked for the structural entropy-bearing model, not yet the final challenge sampler. |
| ROM output distributions | `HAETAE_ROM.ec` distinguishes message, challenge, matrix, and sampler query distributions; the direct-sampler programmed-signing bridge and `31 / 2^58` bad-event bound compile. | Connect query-specific structural distributions to the paper's random-oracle domains and encodings. | Medium: the game-bound mechanics are checked, but domain-separation/encoding correspondence remains. |
| Rejection sampling | Failure terms are concrete nonnegative expressions and no longer zero rewrites. `PaperSimSigningSampler`, `ROMInternalTranscriptPaperSimAsNMA`, `RealSigningPaperSimSampler`, and `HAETAERejectionPaperSimSampler` expose the sampled simulated-signing, real-`sign_internal`, and named rejection/abort boundaries. The attempt state carries separate sampled `y1`, sampled `y2`, commitment, challenge, response, auxiliary response, hint, and abort predicates, and the rejection gate names the C-loop proof surface through `signing_attempt_reject1_bound`, `signing_attempt_reject2_bound`, `signing_attempt_state_reject1_accepts`, `signing_attempt_state_reject2_accepts`, and `signing_attempt_state_pack_accepts`. The sample source is now a checked bounded sampler boundary: `signing_sample_pair_sample_ok` requires well-formedness plus `signing_sample_pair_bounded`, `signing_sample_pair_distribution_ok` consumes that contract, `dstructural_signing_sample_pair` and `dbounded_unit_signing_sample_pair` remain checked instances, and `dsigning_attempt_state_from_sample_pair_sampler` lifts any such sampler into attempt states. The new `dchecked_hyperball_signing_sample_pair` / `dchecked_hyperball_signing_attempt_state` path gives a paper-shaped bounded carrier with checked coefficient-bound support and attempt-state lifting through `checked_hyperball_signing_sample_pair_distribution_ok`, `dchecked_hyperball_signing_attempt_state_lossless`, and `dchecked_hyperball_signing_attempt_state_sample_ok`. This removes the earlier unit-only sampler interface, but the hyperball-shaped carrier is still a bounded product support model, not the exact HAETAE hyperball/sphere sampler or accepted rejection-loop distribution. The paper-sim bridge proves accepted checked rejection attempts reconstruct the same signature fields, and the current structural attempt distribution still has checked losslessness, token-spread, reference-zero-abort, and rejection-zero-abort lemmas. | Replace the current structural attempt-field constructor, deterministic structural `signing_sample_y1/y2`, branch byte, bounded-unit regression source, bounded-product hyperball-shaped carrier, zero-abort reference gate, and `dsigning_attempt_state` distribution facts with the real HAETAE abort/rejection distribution, including exact hyperball/sphere sampling, cardinality/support and point-probability facts, the `reject1` norm bound, the concrete `polyfixveclk_sqnorm2(2z-y)`/branch-bit `reject2` check, hint construction, packing failure probability, and the accepted-loop distribution. Then prove the public-simulator-to-paper-sampler hop from the paper's nonzero abort/rejection loss, lazy-ROM freshness, and challenge-site min-entropy facts. | High: this is a main statistical proof obligation; the checked named rejection sampler is exact only for the current structural model, and the new checked hyperball-shaped carrier proves bounded support rather than the exact HAETAE sampling law or rejection-loop equivalence. |
| Forking extraction | The extractor returns response differences from two valid transcript signatures and proves validity for a fork-specific bimodal target. | Replace the tautological fork-derived target with the paper's bimodal self-target MSIS relation derived from two accepting transcripts at the same programmed challenge site. | High: the current extractor interface is useful plumbing, not the final algebraic extraction theorem. |
| Top-level theorem | `HAETAE_Security.ec` assembles checked ROM/counting paths with explicit remaining premises. The constructed-NMA theorem `euf_cma_security_from_structural_nma_and_counted_bimodal` discharges the old arbitrary clear-site-to-NMA premise using `ROMInternalTranscriptAsNMA` and the checked clear-site bridge. The theorem `euf_cma_security_from_public_sim_nma_and_counted_bimodal` moves the theorem input to `ROMInternalTranscriptPublicSimAsNMA`, whose oracle uses an explicit public-key simulated-signature constructor; `rom_internal_nma_public_sim_exact` proves this public simulator exactly matches the older bridge under the current structural keygen. The theorem `euf_cma_security_from_paper_sim_nma_and_counted_bimodal` routes that path through `ROMInternalTranscriptPaperSimAsNMA` with `rejection_sampling_loss_term`, `euf_cma_security_from_rom_paper_sim_nma_and_counted_bimodal` routes it through the checked ROM-backed structural sampler using `rom_internal_nma_public_sim_rom_paper_sim_exact`, `euf_cma_security_from_real_signing_paper_sim_nma_and_counted_bimodal` routes it through the checked real-`sign_internal` sampled boundary using `rom_internal_nma_real_signing_paper_sim_exact`, and `euf_cma_security_from_haetae_rejection_paper_sim_nma_and_counted_bimodal` routes it through the named current-model rejection boundary using `rom_internal_nma_real_signing_haetae_rejection_paper_sim_exact`. | Replace the structural public-seed-determines-signing-carriers equivalence and the current `haetae_rejection_sample_from_attempt` implementation with the paper's adversary-level simulated-signing/forking reduction, then remove all non-hardness proof-boundary premises so the final theorem assumes only standard MLWE/Module-SIS hardness games and concrete parameter/query facts. | High: current security theorem is conditional, though the NMA boundary no longer exposes a secret-reconstructing signing oracle as the top-level counted theorem input and now has explicit sampled-simulator, ROM-backed structural-sampler, real-`sign_internal`, and named rejection-sampler boundaries. |

Current KAT update: the public-key/FIPS202 row's earlier two-bit note is now
superseded by a compiled complete output-byte-0 final-round slice in
`HAETAE_FIPS202_KAT_Empty_Generated.ec`: eight 33-premise round-23-to-round-24
bit lemmas plus `empty_round24_output_byte00_from_expected_bits`. The first
twenty-two byte-0 prerequisites are also pushed one hop backward: six bit-0 facts
from `empty_round23_lane00_bit00_from_round22_bits` through
`empty_round23_lane24_bit00_from_round22_bits`, and five bit-63 facts from
`empty_round23_lane01_bit63_from_round22_bits` through
`empty_round23_lane21_bit63_from_round22_bits`, plus eleven bit-20 facts from
`empty_round23_lane06_bit20_from_round22_bits` spanning through
`empty_round23_lane23_bit20_from_round22_bits`, plus eleven bit-19/bit-21 facts:
`empty_round23_lane02_bit19_from_round22_bits`,
`empty_round23_lane07_bit19_from_round22_bits`,
`empty_round23_lane12_bit19_from_round22_bits`,
`empty_round23_lane17_bit19_from_round22_bits`,
`empty_round23_lane22_bit19_from_round22_bits`,
`empty_round23_lane12_bit21_from_round22_bits`,
`empty_round23_lane01_bit21_from_round22_bits`,
`empty_round23_lane06_bit21_from_round22_bits`,
`empty_round23_lane11_bit21_from_round22_bits`,
`empty_round23_lane16_bit21_from_round22_bits`,
and `empty_round23_lane21_bit21_from_round22_bits`. The full set of `shake256_empty_round24`
output-bit premises (all 512 output-bit ones from round 24, i.e. 64 output bytes)
are now generated, with all corresponding concrete final-output byte bridges
(`empty_round24_output_byteNN_from_expected_bits`) now present. The remaining KAT
work is propagating the backward round-premise chain from these round-24 facts to
the padded input and connecting these concrete Keccak lemmas to the reference
memory-model constraints used by the C/Jasmin layer.

Next proof work should target the remaining concrete-algebra boundary: prove
actual reference-C FIPS202 byte equivalence, the C/Jasmin alignment of the padded
SHAKE chain, and backward round-premise connection through concrete Keccak for all
bytes, then connect the structural mode-shaped public key to real HAETAE
rejection-loop sampling, NTT,
and rounding definitions; replace
the structural challenge sampler with the full transcript challenge sampler;
then feed those equations into the forking extractor and Module-SIS target.
